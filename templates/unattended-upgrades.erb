// file managed by Puppet
Unattended-Upgrade::Allowed-Origins {
<% Array(scope.lookupvar("baserepos")).each do |repo| -%>
        "<% repo -%>";
<% end -%>
<% if has_variable?("extrarepos") then -%>
<% Array(scope.lookupvar("extrarepos")).each do |repo| -%>
        "<% repo -%>";
<% end -%>
<% end -%>
};

Dpkg::Options { "--force-confold"; }
